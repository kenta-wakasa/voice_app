import 'package:flutter/material.dart';

class AnalyisPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: const Text('hoge')),
    );
  }
}
